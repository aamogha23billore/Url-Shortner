<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Analytics - <%= shortId %></title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #fafafa;
      padding: 20px;
    }
    h1 {
      color: #333;
    }
    .stats {
      background: #fff;
      padding: 15px;
      border-radius: 8px;
      box-shadow: 0 2px 6px rgba(0,0,0,0.1);
      margin-top: 20px;
    }
    ul {
      padding-left: 20px;
    }
  </style>
</head>
<body>
  <h1>Analytics for <%= shortId %></h1>
  <div class="stats">
    <p><strong>Total Clicks:</strong> <%= totalClicks %></p>
    <h3>Visit History:</h3>
    <ul>
      <% analytics.forEach(hit => { %>
        <li><%= new Date(hit.timestamp).toLocaleString() %></li>
      <% }) %>
    </ul>
  </div>
  <p><a href="/">⬅ Back to Home</a></p>
</body>
</html>
